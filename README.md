# UnityURP-UDIM

Because Unity still does not have native support for UDIM...

This shader is a very simple URP lit shader that supports a row of UDIM tiles by storing them in a Texture2DArray. It isn't a full PBR shader. instead it only performs diffuse Lambert lighting with shadows
